services:
  # 1. Backend API Service (Node.js & Express)
  backend:
    image: 058835008914.dkr.ecr.ap-south-1.amazonaws.com/backend_repo:latest
    container_name: url-shortener-backend
    environment:
      - PORT=5000
      - BASE_URL=http://13.232.205.135:3000
    ports:
      - "5000:5000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://127.0.0.1:5000/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 5s
    restart: unless-stopped
    networks:
      - app-network

  # 2. Frontend Web Service (React, Vite, Nginx)
  frontend:
    image: 058835008914.dkr.ecr.ap-south-1.amazonaws.com/frontend_repo:latest
    container_name: url-shortener-frontend
    ports:
      - "3000:80"
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "wget --no-verbose --tries=1 --spider http://127.0.0.1:80/ || exit 1"
        ]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 5s
    restart: unless-stopped
    networks:
      - app-network

# Isolated bridge network for inter-container communication
networks:
  app-network:
    driver: bridge
